// Command chess-server is the SSR HTTP server stub.
// Full implementation is in a future user story.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "chess-server: not yet implemented")
	os.Exit(1)
}
